sudo: required

language: node_js
cache:
  directories:
    - "node_modules"

before_install:
  # APT
  - sudo apt -qq update
  - sudo apt install -y expect

before_script:
  - chmod +x generate.exp
  - ./generate.exp
  - cd test
  - npm ci
  - npm run generate